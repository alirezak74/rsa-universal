import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'RSA DEX - Decentralized Exchange for RSA Chain',
  description: 'Trade RSA CRYPTO and other assets on the RSA Chain decentralized exchange',
  keywords: 'dex, trading, rsa-chain, cryptocurrency, exchange, rsa crypto',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50 dark:bg-gray-900">
          {children}
        </div>
      </body>
    </html>
  )
} 